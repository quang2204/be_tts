import { Router } from "express";

const routes = Router();

routes.use("/products", console.log("ddd"))
// routes.use("/products", hanldeProduct...)
// routes.use("/products", hanldeProduct...)
// routes.use("/products", hanldeProduct...)
// routes.use("/products", hanldeProduct...)

export default routes;
